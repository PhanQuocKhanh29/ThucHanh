@model IEnumerable<BTL_LTQL.Models.ChiDoan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenChiDoan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenChiDoan)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDChiDoan }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDChiDoan }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDChiDoan })
        </td>
    </tr>
}

</table>
